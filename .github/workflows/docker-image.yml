name: build
on:  
  push:
    tags:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v2
    - name: env
      run: |
        set -x
        grep "=" ".env.template" | sed -e '/^#/d;/^\s*$/d' >> "${GITHUB_ENV}"
        #
        echo "TARGET_VERSION=$(echo ${GITHUB_REF:10})" >> $GITHUB_ENV
        echo "TARGET_BUILD=${{ github.run_number }}" >> $GITHUB_ENV
        echo "TARGET_REGISTRY_TOKEN=${{ secrets.GITHUB_TOKEN }}" >> $GITHUB_ENV
        echo "TARGET_REGISTRY_USER=${{ github.actor }}" >> $GITHUB_ENV
        #
        echo "INFO: GITHUB_ENV:"
        cat "${GITHUB_ENV}"
    - name: login
      run: |
        set -x
        make login
    - name: build
      run: |
        set -x
        make build
    - name: publish
      run: |
        set -x
        make publish
    - name: logout
      run: |
        set -x
        make logout

